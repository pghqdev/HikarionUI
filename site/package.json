{
  "name": "hikarion-site",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "bun run sync-assets.mjs && bun ../scripts/og-image.mjs",
    "dev": "bun run sync-assets.mjs && bun ../scripts/og-image.mjs && astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.0.7"
  }
}
